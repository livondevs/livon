# 🚀 Release Notes (<%= Time.now.strftime('%Y-%m-%d') %>)

<% fix_prs = pull_requests.select { |pr| pr.labels.include?('fix') } %>
<% if fix_prs.any? %>
## 🛠 Bug Fixes (Fix)
<% fix_prs.each do |pr| %>
  - [#<%= pr.number %>] <%= pr.title %> (@<%= pr.author %>)
<% end %>
<% end %>

<% feature_prs = pull_requests.select { |pr| pr.labels.include?('feature') } %>
<% if feature_prs.any? %>
## ✨ New Features (Feature)
<% feature_prs.each do |pr| %>
  - [#<%= pr.number %>] <%= pr.title %> (@<%= pr.author %>)
<% end %>
<% end %>

<% refactor_prs = pull_requests.select { |pr| pr.labels.include?('refactor') } %>
<% if refactor_prs.any? %>
## 🔨 Refactoring (Refactor)
<% refactor_prs.each do |pr| %>
  - [#<%= pr.number %>] <%= pr.title %> (@<%= pr.author %>)
<% end %>
<% end %>

<% version_prs = pull_requests.select { |pr| pr.labels.include?('version') } %>
<% if version_prs.any? %>
## 📌 Version Updates (Version)
<% version_prs.each do |pr| %>
  - [#<%= pr.number %>] <%= pr.title %> (@<%= pr.author %>)
<% end %>
<% end %>

<% chore_prs = pull_requests.select { |pr| pr.labels.include?('chore') } %>
<% if chore_prs.any? %>
## 🔧 Other Changes (Chore)
<% chore_prs.each do |pr| %>
  - [#<%= pr.number %>] <%= pr.title %> (@<%= pr.author %>)
<% end %>
<% end %>

<% others = pull_requests.select { |pr| !pr.labels.any? { |label| ['fix', 'feature', 'refactor', 'version', 'chore'].include?(label) } } %>
<% if others.any? %>
## Others
<% others.each do |pr| %>
  - [#<%= pr.number %>] <%= pr.title %> (@<%= pr.author %>)
<% end %>
<% end %>